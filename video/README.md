# ServicePilot AI Demo Video

这是 ServicePilot AI 的 180 秒 Ink Press 风格 Remotion 工程。完整制作说明、交付物路径、音频署名和终检基线见 [`../docs/VIDEO.md`](../docs/VIDEO.md)。

```powershell
npm install
npm run typecheck
npm run render
```

最终成片生成到 `out/servicepilot-ai-demo-final.mp4`；`out/` 不纳入 Git，发布时上传 GitHub Release。
